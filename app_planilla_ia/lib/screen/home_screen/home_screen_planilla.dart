import 'package:flutter/material.dart';

class HomeScreenPlanilla extends StatelessWidget {
  const HomeScreenPlanilla({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}